import { TooltipBasicDirective } from './tooltip-basic.directive';

describe('TooltipBasicDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipBasicDirective();
    expect(directive).toBeTruthy();
  });
});
